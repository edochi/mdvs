# Quick Thought

Sometimes you just want to jot down a quick idea without any metadata.

This note has no frontmatter at all. It should still be indexed — the promoted columns should be NULL and the metadata should be empty JSON.

The content should still be chunked and embedded normally.
