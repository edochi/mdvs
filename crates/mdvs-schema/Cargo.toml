[package]
name = "mdvs-schema"
version = "0.1.0"
edition = "2024"
description = "Field definitions, type system, and frontmatter field discovery for mdvs"

[dependencies]
chrono = "0.4"
serde_json = "1"
