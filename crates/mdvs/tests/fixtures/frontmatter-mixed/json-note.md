{
  "title": "JSON note",
  "author": "carol",
  "year": 2024,
  "tags": ["json", "example"]
}

# Body

A note written with Hugo-style JSON frontmatter. Auto-detect picks JSON
because the file starts with `{`. The body follows the matching `}`.
