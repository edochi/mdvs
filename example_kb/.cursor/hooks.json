{
  "_comment": "mdvs PostToolUse hooks generated by `mdvs scaffold hook --platform cursor`. Merge into .cursor/hooks.json under the existing `hooks` key. Both hooks self-scope by walking up from the agent's cwd to find mdvs.toml; they stay silent outside an mdvs vault.",
  "hooks": {
    "postToolUse": [
      {
        "command": "mdvs hook handle --platform cursor --kind validate",
        "matcher": "Edit|Write|MultiEdit"
      },
      {
        "command": "mdvs hook handle --platform cursor --kind search-nudge",
        "matcher": "Bash"
      }
    ]
  },
  "version": 1
}
