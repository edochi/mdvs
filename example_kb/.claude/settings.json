{
  "_comment": "mdvs PostToolUse hooks generated by `mdvs scaffold hook --platform claude-code`. Merge into .claude/settings.json under the existing `hooks` key. Both hooks self-scope by walking up from the agent's cwd to find mdvs.toml; they stay silent outside an mdvs vault.",
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "mdvs hook handle --platform claude-code --kind validate",
            "type": "command"
          }
        ],
        "matcher": "Edit|Write|MultiEdit"
      },
      {
        "hooks": [
          {
            "command": "mdvs hook handle --platform claude-code --kind search-nudge",
            "type": "command"
          }
        ],
        "matcher": "Bash"
      }
    ]
  }
}
