---
id: 43
title: Fix tracing levels — distinct debug/trace events with elapsed times
status: todo
priority: medium
created: 2026-03-05
depends_on: [34]
blocks: []
---

# TODO-0043: Fix tracing levels — distinct debug/trace events with elapsed times

## Summary

Independent from output formatting. Currently debug and trace produce identical output, and neither shows elapsed times. Add distinct events at each level.

## Changes

### Debug level (`--logs=debug`)
- Per-file classify details (new/edited/unchanged/removed) in build
- Per-file scan hits in discover
- Per-file embed timing

### Trace level (`--logs=trace`)
- SQL queries in search/backend
- Arrow schema details
- Chunking decisions (chunk boundaries, sizes)

### Elapsed times
- Add `elapsed_ms` to debug/trace events where useful
- Use `std::time::Instant` for per-operation timing

## Files to modify

- `src/cmd/build.rs` — per-file classify/embed debug events
- `src/discover/scan.rs` — per-file scan debug events
- `src/index/backend.rs` — SQL trace events
- `src/index/chunk.rs` — chunking trace events
- `src/index/embed.rs` — per-file embed debug events
