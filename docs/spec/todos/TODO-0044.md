---
id: 44
title: Final distribution readiness and cargo-dist setup
status: todo
priority: high
created: 2026-03-06
depends_on: []
blocks: []
---

# TODO-0044: Final distribution readiness and cargo-dist setup

## Summary

Prepare the repository for official publication on crates.io and configure automated cross-platform binary distribution (macOS, Linux, Windows) using `cargo-dist`, including npm and Homebrew integration.

## Changes

### Metadata & Crate Optimization
- Update `Cargo.toml` to include `homepage`, `documentation`, and `repository`.
- Replace the broad `exclude` list with a surgical `include` list to guarantee a minimal source package (under 1MB).
- Ensure `LICENSE` and `README.md` are explicitly included in the package.

### Automated Release Workflow (`cargo-dist`)
- Initialize `cargo-dist` to generate GitHub Action workflows.
- Configure build targets: `aarch64-apple-darwin` (Mac M1+), `x86_64-apple-darwin` (Intel Mac), `x86_64-unknown-linux-gnu` (Linux), and `x86_64-pc-windows-msvc` (Windows).
- Enable **Homebrew** formula generation for automated tap updates.
- Enable **npm** package generation for `npx mdvs` support.

### Documentation Update
- Update `README.md` "Install" section to reflect the new options:
  - `brew install edochi/tap/mdvs`
  - `npx mdvs init`
  - Pre-built binary install script (curl | sh).

## Files to modify

- `Cargo.toml` — Update metadata and package inclusion rules.
- `README.md` — Update installation instructions.
- `.github/workflows/release.yml` — (To be created by `cargo-dist`).
- `dist-workspace.toml` — (To be created by `cargo-dist`).

## Technical Notes

- `cargo-dist` requires a GitHub personal access token (PAT) for some advanced features, but the basic initialization can be done locally.
- Use `cargo publish --dry-run` to verify the final `.crate` file size before actual publication.
- The npm package will act as a binary wrapper, detecting the platform and downloading the correct artifact from GitHub Releases.
