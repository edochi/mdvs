---
id: 53
title: "Handle symlink escape in scan strip_prefix"
status: todo
priority: high
created: 2026-03-07
depends_on: []
blocks: []
---

# TODO-0053: Handle symlink escape in scan strip_prefix

## Details

In `scan.rs:54`, `.unwrap()` on `strip_prefix(root)` will panic if a symlink causes a file's absolute path to be outside the root directory. Should skip the file with a warning or return an error.

## Files

- `src/discover/scan.rs` — replace `.unwrap()` with proper error handling
