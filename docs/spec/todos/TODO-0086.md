---
id: 86
title: "Rework info command pipeline"
status: done
priority: medium
created: 2026-03-08
completed: 2026-03-08
depends_on: [79, 80]
blocks: []
files_created:
  - src/pipeline/read_index.rs
files_updated:
  - src/cmd/info.rs
  - src/main.rs
---

# TODO-0086: Rework info command pipeline

## Summary

Rework the info command to use the pipeline model.

## Resolution

Reworked `info::run()` to return `InfoCommandOutput` directly (no `anyhow::Result`). Pipeline is `read_config → scan → read_index` (3 steps — scan needed for file/field counts). Created `read_index` pipeline step module with `run_read_index()`, `ReadIndexOutput`, and `IndexData`. "No index found" is a normal `Completed` output. Removed `glob` and `elapsed_ms` from `InfoResult`. Exit code 2 for failed steps.
