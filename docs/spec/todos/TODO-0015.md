---
id: 15
title: Implement info and clean commands
status: todo
priority: high
created: 2026-03-02
depends_on: [12]
blocks: []
---

# TODO-0015: Implement info and clean commands

## Summary

Replace the `todo!()` stubs for info and clean commands with working implementations.

## Details

### Clean
- Delete `.mdvs/` directory if it exists
- Do not modify `mdvs.toml`
- Print what was deleted (or "nothing to clean")

### Info
- Read `mdvs.toml` config
- Scan files (count only)
- If `.mdvs/` exists: read parquet metadata (from TODO-0012) and file/chunk row counts
- Display scan config, fields with constraints, ignored fields, index status
- `config_match`: compare toml config vs parquet metadata
- Implement `InfoResult` and `CommandOutput` trait
- Support `--output json`
