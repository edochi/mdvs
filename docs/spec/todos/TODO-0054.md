---
id: 54
title: "Handle invalid glob pattern without panicking"
status: done
priority: high
created: 2026-03-07
depends_on: []
blocks: []
---

# TODO-0054: Handle invalid glob pattern without panicking

## Details

In `scan.rs:34`, `.expect("invalid glob pattern")` panics on an invalid user-provided glob pattern from `mdvs.toml`. Should return an `anyhow::Result` with a message showing the invalid pattern.

## Files

- `src/discover/scan.rs` — replace `.expect()` with `?` and `.context()`
