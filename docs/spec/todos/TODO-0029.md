---
id: 29
title: User documentation site with mdBook
status: in-progress
priority: high
created: 2026-03-03
depends_on: []
blocks: []
---

# TODO-0029: User documentation site with mdBook

## Summary

Create a user-facing documentation site using mdBook, deployed to GitHub Pages. Uses `example_kb/` as the running example throughout. The README covers installation and quick start; the docs site covers full usage, configuration reference, and features in action.

## Key decisions

- **Location:** `book/` at repo root (not `docs/book/`). `docs/` is implementor-facing specs; `book/` is user-facing documentation. Different audience, different purpose.
- **Running example:** All pages use `example_kb/` (the Prismatiq Lab fixture) for examples and output snippets. This keeps examples consistent and testable.
- **Concepts split into sub-pages:** `concepts.md` is a hub linking to 4 focused sub-pages: types & widening, schema inference, validation, search & indexing. Each topic is dense enough (widening matrix, inference algorithm, violation types) to warrant its own page with examples. `search-guide.md` still covers `--where` syntax and ranking together.
- **Query examples in depth:** `search-guide.md` must include detailed, runnable examples for every query pattern: scalar filters, array containment (`array_has`, `= ANY()`), array length, nested object bracket access, field names with special characters, and combined filters. All examples against `example_kb/` so readers can copy-paste and try them. See `scripts/test_array_queries.rs` for tested query patterns to draw from.
- **Writing conventions:** see the `book` skill (`.claude/skills/book/SKILL.md`).

## Checklist

### Scaffold

- [x] Initialize `book/` with `mdbook init`
- [x] Create `SUMMARY.md` with page structure
- [x] Create stub files for all pages
- [x] Add `book` skill with writing conventions

### Tier 1 — Core (read first)

- [x] Write `introduction.md` — what mdvs is, the "DB of documents" pitch, database analogy
- [x] Write `getting-started.md` — install, clone example_kb, `init`, `search`. Captured output from real runs.
- [ ] Write `concepts.md` — hub page linking to sub-pages, quick summary of each concept area
- [ ] Write `concepts/types.md` — FieldType enum, type widening matrix, nullable, examples from example_kb
- [ ] Write `concepts/schema.md` — scan → inference → globs, allowed/required, bare files, ignore list, field states
- [ ] Write `concepts/validation.md` — violation types, check behavior, String as top type, exit codes
- [ ] Write `concepts/search.md` — chunking, embedding models, incremental build, ranking, --where overview

### Tier 2 — Commands

- [ ] Write `commands/init.md` — synopsis, flags, behavior, examples with captured output
- [ ] Write `commands/check.md`
- [ ] Write `commands/update.md`
- [ ] Write `commands/build.md`
- [ ] Write `commands/search.md`
- [ ] Write `commands/info.md`
- [ ] Write `commands/clean.md`

### Tier 3 — Reference

- [ ] Write `configuration.md` — full `mdvs.toml` reference, all sections, all fields, defaults. Annotate using example_kb's toml.
- [ ] Write `search-guide.md` — `--where` deep dive: scalar, array, nested object, special characters, ranking. All queries runnable against example_kb.

### Tier 4 — Recipes

- [ ] Write `recipes/obsidian.md` — setting up mdvs on an Obsidian vault, `.mdvsignore`, typical patterns
- [ ] Write `recipes/ci.md` — running `mdvs check` in CI, exit codes, JSON output, example GitHub Actions workflow

### Finalize

- [ ] GitHub Actions workflow to build and deploy to GitHub Pages on push to main
- [ ] Delete old book (`docs/book/`)
- [ ] Link from README: "Full documentation at ..."
