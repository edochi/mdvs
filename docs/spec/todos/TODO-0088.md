---
id: 88
title: "main.rs error handling and exit codes"
status: todo
priority: medium
created: 2026-03-08
depends_on: [81, 82, 83, 84, 85, 86, 87]
blocks: []
---

# TODO-0088: main.rs error handling and exit codes

## Summary

Rework `main.rs` to handle the new pipeline-based command outputs with consistent error rendering and exit codes.

## Details

### Exit codes

- **0**: pipeline completed, result present (success)
- **1**: pipeline completed, normal result with actionable findings (violations in check/build)
- **2**: user error — bad input, missing config, model mismatch (4xx)
- **3**: application error — I/O failure, internal error (5xx)

### Error rendering

When a command returns a `*CommandOutput` with a failed step:
- `--output text`: render completed steps as lines, render the failed step's error message, render skipped steps if verbose
- `--output json`: serialize the full `*CommandOutput` including process steps and null result

### Changes

- Remove `anyhow::Result` as the top-level return from commands (or catch and convert)
- Each command returns its `*CommandOutput` directly — errors are inside the pipeline
- `main.rs` inspects the output to determine exit code
- Current `?` propagation for unexpected errors → catch at top level, convert to application error

### Open questions

- Should commands still return `anyhow::Result<*CommandOutput>` for truly catastrophic errors (OOM, etc.), with the pipeline handling everything else?
- How to convert existing `anyhow::bail!()` calls to `ProcessingStepError` — wrapper type? `From` impl?

## Files

- `src/main.rs` — dispatch, exit codes, error rendering
