---
id: 60
title: "Add tests for update command"
status: done
priority: high
created: 2026-03-07
depends_on: []
blocks: []
---

# TODO-0060: Add tests for update command

## Details

`cmd/update.rs` has zero tests. This is a critical command (re-inference, field management). Needs tests for:

- New fields only (default mode)
- `--reinfer <field>` re-infers named field, keeps others
- `--reinfer-all` re-infers everything
- Type widening on update (Integer → Float)
- Ghost fields (field disappears from all files during reinfer)
- `auto_build` triggers build after update
- Glob-only changes detected during reinfer

## Files

- `src/cmd/update.rs` — add test module
