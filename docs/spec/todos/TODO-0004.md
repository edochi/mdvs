---
id: 4
title: "Rename --where-clause to --where"
status: todo
priority: low
created: 2026-03-02
depends_on: []
blocks: []
---

# TODO-0004: Rename --where-clause to --where

## Summary

Rename the search `--where-clause` flag to `--where` to match the spec.

## Details

The clap field `where_clause` auto-derives `--where-clause` as the long flag. The spec says `--where`.

**Fix:** Change `#[arg(long, name = "where")]` to `#[arg(long = "where", name = "where")]` in the Search command definition.

**Files:** `src/main.rs`
