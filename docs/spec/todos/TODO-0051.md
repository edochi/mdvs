---
id: 51
title: "Replace panic! in model loading with error propagation"
status: done
priority: high
created: 2026-03-07
depends_on: []
blocks: []
---

# TODO-0051: Replace panic! in model loading with error propagation

## Details

In `embed.rs:46`, `Embedder::load()` uses `unwrap_or_else(|e| panic!(...))` on model loading failure. This crashes the process with a stacktrace instead of returning a helpful error. Should return `anyhow::Result` and propagate the error.

## Files

- `src/index/embed.rs` — change `Embedder::load()` signature to return `Result`
- `src/cmd/build.rs` — update call sites
- `src/cmd/search.rs` — update call sites
