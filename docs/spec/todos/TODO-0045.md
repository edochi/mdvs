---
id: 45
title: cargo-dist initialization and release workflow
status: done
priority: high
created: 2026-03-06
depends_on: [44]
blocks: [46, 47]
---

# TODO-0045: cargo-dist initialization and release workflow

## Summary

Initialize `cargo-dist` to generate GitHub Actions release workflows. Configure build targets for cross-platform binary distribution: `aarch64-apple-darwin` (Mac M1+), `x86_64-apple-darwin` (Intel Mac), `x86_64-unknown-linux-gnu` (Linux), `x86_64-pc-windows-msvc` (Windows).

## Changes

- Install and run `cargo dist init`
- Configure build targets in `dist-workspace.toml`
- Generate `.github/workflows/release.yml`
- Tag-based release trigger (push `v*` tag → build + publish)

## Files to modify

- `dist-workspace.toml` — created by cargo-dist
- `.github/workflows/release.yml` — created by cargo-dist
- `Cargo.toml` — cargo-dist may add metadata
