---
id: 47
title: npm binary wrapper via cargo-dist
status: todo
priority: medium
created: 2026-03-06
depends_on: [45]
blocks: []
---

# TODO-0047: npm binary wrapper via cargo-dist

## Summary

Enable npm package generation in cargo-dist. The npm package acts as a binary wrapper — detects the platform and downloads the correct prebuilt binary from GitHub Releases. Enables `npx mdvs` usage.

## Changes

- Enable npm installer in `dist-workspace.toml`
- Configure npm package name and scope
- Verify `npx mdvs` works after a release

## Files to modify

- `dist-workspace.toml` — enable npm installer
