---
id: 35
title: Add tabled + terminal_size and create table style helpers
status: done
priority: high
created: 2026-03-05
depends_on: [34]
blocks: [36, 37, 38, 39, 40, 41, 42]
---

# TODO-0035: Add tabled + terminal_size and create table style helpers

## Summary

Add table rendering dependencies and create reusable style helpers for all command output.

## Changes

- Add `tabled = "0.20"` and `terminal_size = "0.4"` to `Cargo.toml`
- Create table helper module (in `src/output.rs` or `src/table.rs`)
- `style_compact()`: `Style::rounded()` + `remove_horizontals()` + `Width::increase(w)` + `Width::wrap(w)`
- `style_record(cols)`: `Style::rounded()` + `ColumnSpan` on detail row + `BorderCorrection` + `Width::increase(w)` + `Width::wrap(w)`
- `term_width()` helper using `terminal_size` crate (fallback 80)

## Prototype

See `scripts/test_tables.rs` for working examples of both styles.

## Files to modify

- `Cargo.toml`
- `src/output.rs` (or new `src/table.rs`)
