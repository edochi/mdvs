---
id: 25
title: Trim published package size
status: todo
priority: low
created: 2026-03-03
depends_on: []
blocks: []
---

# TODO-0025: Trim published package size

## Summary

The crate packages 106 files (837KB) including spec archives, TODO files, and test fixtures that users don't need. Add `exclude` to Cargo.toml to reduce the download.

## Details

Add to `[package]` in Cargo.toml:

```toml
exclude = ["docs/", "tests/fixtures/", ".claude/", "scripts/"]
```

Verify with `cargo package --list` that only source, README, LICENSE, and Cargo.toml are included.
