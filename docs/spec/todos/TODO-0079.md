---
id: 79
title: "Core pipeline abstractions"
status: done
priority: medium
created: 2026-03-08
completed: 2026-03-08
depends_on: []
blocks: [80, 81, 82, 83, 84, 85, 86, 87, 88]
files_created:
  - src/pipeline/mod.rs
---

# TODO-0079: Core pipeline abstractions

## Summary

Define the foundational types for the step-based pipeline model in a new `src/pipeline/mod.rs` module.

## Resolution

Implemented `StepOutput` trait, `ProcessingStep<T>`, `ProcessingStepResult<T>` (internally tagged enum with `Completed`/`Failed`/`Skipped`), `ProcessingStepError`, and `ErrorKind` in `src/pipeline/mod.rs`. Includes 7 tests for JSON serialization shapes and `format_line()` rendering. Trait bound is `Serialize` (not `StepOutput` supertrait) — both bounds written explicitly where needed.
